description: LMT sim868e driver

compatible: "lmt,sim868e"

include: [base.yaml]

properties:

    label:
      required: false

    mdm-power-gpios:
      type: phandle-array
      required: true

    mdm-pwkey-gpios:
      type: phandle-array
      required: true

    mdm-status-gpios:
      type: phandle-array
      required: true
